apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "puhatak-backend.fullname" . }}-postgres-data
  labels:
    app.kubernetes.io/name: {{ include "puhatak-backend.name" . }}-postgres
    helm.sh/chart: {{ include "puhatak-backend.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: {{ .Values.db.storage.storageClass }}
  resources:
    requests:
      storage: {{ .Values.db.storage.size }}
