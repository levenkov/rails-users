apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "pm-backend.fullname" . }}-postgres-data
  labels:
    app.kubernetes.io/name: {{ include "pm-backend.name" . }}-postgres
    helm.sh/chart: {{ include "pm-backend.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: {{ .Values.db.storage.storageClass }}
  resources:
    requests:
      storage: {{ .Values.db.storage.size }}
